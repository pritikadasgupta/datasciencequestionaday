# https://twitter.com/data_question

# When u try to load data like AirPassengers in R, 
# using the function data(), the dataset is loaded as a Promise, 
# rather than a dataframe. 

# What is Promise?